name: ci

on:
  push:
  pull_request:

jobs:
  agent-pack-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: AgentPack validation gate
        run: make validate
