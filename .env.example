# Environment Variables Template (.env.example)
#
# HARD RULE:
# - Do NOT commit real secrets/tokens/private data.
# - This file must contain only variable names + documentation + placeholders.
#
# TODO:
# - Add project-specific variables here (name, purpose, required/optional, source).
# - Mirror the canonical list in `agent_pack/PROJECT_MEMORY.md -> Engineering & Run -> Env Vars`.

# EXAMPLE (placeholder only; replace with real project vars later)
# SOME_VARIABLE="__PLACEHOLDER__"

